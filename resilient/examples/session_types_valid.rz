// RES-4268: Valid session protocol declaration and call sequence

#[session(protocol = "send(int).recv(bool).close")]
fn channel_step() -> int {
    return 0;
}

fn main() {
    println(channel_step());
    println(channel_step());
    println(channel_step());
}

main();
