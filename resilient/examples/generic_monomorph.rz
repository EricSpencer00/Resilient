fn identity<T>(T x) -> T {
    return x;
}

fn first<A, B>(A a, B b) -> A {
    return a;
}

fn main() {
    println(identity(42));
    println(identity("hello"));
    println(identity(true));
    println(first(1, "ignored"));
}

main();
