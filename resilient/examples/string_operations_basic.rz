// RES-3178: string_operations regression corpus — string manipulation

fn main() {
    let s = "hello";
    let len = s.len();
    println(len);

    let concat = concat(s, " world");
    println(concat);
}
