# Requires a locally-built companion library (see the header comment in
# ffi_array_sum.rz) and a binary built with `--features ffi`. The library
# name is platform-specific (libsum.so / libsum.dylib), so this example
# is exempt from the golden-file audit via this .interactive marker.
#
# The same call shapes ARE exercised end-to-end in CI by
# resilient/tests/it/ffi_integration.rs, against a helper library that
# build.rs compiles from resilient/tests/ffi/lib_testhelper.c.
