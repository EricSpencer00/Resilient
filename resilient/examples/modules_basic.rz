// RES-3160: modules regression corpus — module system

mod math {
    pub fn add(a: i32, b: i32) -> i32 {
        return a + b;
    }
}

fn main() {
    let result = math::add(10, 20);
    println(result);
}
