#[mutual_tail_call]
fn is_even(Int n) -> bool {
    if (n == 0) { return true; }
    return is_odd(n - 1);
}
#[mutual_tail_call]
fn is_odd(Int n) -> bool {
    if (n == 0) { return false; }
    return is_even(n - 1);
}
println(is_even(10));
println(is_odd(7));
println(is_even(100000));
