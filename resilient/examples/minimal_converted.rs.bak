// Minimal working example for the Resilient language

// A simple function that adds one to its input
fn add_one(int x) {
    return x + 1;
}

// Print a message to the console
fn print_message(string msg) {
    println(msg);
}

// Main function - program entry point
fn main(int dummy) {
    print_message("Starting the program...");
    
    // Calculate and display result
    let result = add_one(41);
    println("The answer is: " + result);
    
    print_message("Program completed.");
}

// Call the main function to start execution
main(0);
