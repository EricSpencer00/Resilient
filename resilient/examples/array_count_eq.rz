// RES-478 demo: array_count_eq is an explicit-name alias for array_count.

fn main() {
    println(array_count_eq([1, 2, 1, 3, 1, 2], 1));
    println(array_count_eq([1, 2, 3], 99));
    println(array_count_eq(["the", "and", "the", "of"], "the"));
}

main();
