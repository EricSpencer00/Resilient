# exit: 1
# stderr:
./examples/region_infer_unannotated_alias_err.rz:16:14: E: call to `set_both` passes `x` as 2 simultaneous reference arguments (at least one `&mut`) — the same binding cannot be both aliased and exclusively borrowed at once
./examples/region_infer_unannotated_alias_err.rz:16:14: E: call to `set_both` passes `x` as 2 simultaneous reference arguments (at least one `&mut`) — the same binding cannot be both aliased and exclusively borrowed at once
Borrow check: E: call to `set_both` passes `x` as 2 simultaneous reference arguments (at least one `&mut`) — the same binding cannot be both aliased and exclusively borrowed at once
       set_both(x, x);
                ^
Error: Borrow check failed: 1 error(s)
