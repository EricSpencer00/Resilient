// RES-3179: vector_operations regression corpus — vector iteration

fn sum_vec(v: Vec<i32>) -> i32 {
    let mut sum = 0;
    for item in v {
        sum = sum + item;
    }
    return sum;
}

fn main() {
    let v = vec![1, 2, 3, 4];
    println(sum_vec(v));
}
