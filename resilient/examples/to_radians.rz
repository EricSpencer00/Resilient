// RES-894 demo: to_radians(d) converts degrees to radians.

fn main(int _d) {
    println(to_radians(0.0));      // 0
    println(to_radians(360.0));    // 2π ≈ 6.28318...
    // sin(to_radians(90)) should round-trip to 1.
    println(sin(to_radians(90.0))); // 1

    return 0;
}

main(0);
