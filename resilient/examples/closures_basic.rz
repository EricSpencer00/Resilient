// RES-3163: closures regression corpus — closure basics

fn apply_closure<F: Fn(i32) -> i32>(f: F, x: i32) -> i32 {
    return f(x);
}

fn main() {
    let add_five = |x| { return x + 5; };
    let result = apply_closure(add_five, 10);
    println(result);
}
