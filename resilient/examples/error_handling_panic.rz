// RES-3170: error_handling regression corpus — panic and recovery

fn divide(a: i32, b: i32) -> i32 {
    if b == 0 {
        panic("division by zero");
    }
    return a / b;
}

fn main() {
    let result = divide(10, 2);
    println(result);
}
