// RES-369: struct patterns in match — catch-all via `Point { .. }`.

struct Point {
    int x,
    int y,
}

fn main() {
    let p = new Point { x: 1, y: 2 };
    let r = match p {
        Point { x: 0, y: 0 } => 0,
        Point { .. } => 1,
    };
    println(r);
}

main();
