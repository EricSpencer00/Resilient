// RES-3177: clone_copy regression corpus — Copy semantics

#[derive(Copy)]
struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p1 = Point { x: 10, y: 20 };
    let p2 = p1;  // Copy, not move
    println(p1.x);
}
