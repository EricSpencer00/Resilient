*/target/*
*.
!*/
resilient/target
resilient-runtime/target
resilient-runtime-cortex-m-demo/target
resilient-span/target

# Ralph-loop runtime artifacts (logs grow, STOP is a local kill switch)
.board/logs/
.board/STOP
# Native benchmark binaries — built by benchmarks/run.sh on demand.
benchmarks/fib/fib_native
benchmarks/sum/sum_native

# Ralph-loop session state (local to each Claude Code run)
.claude/
.worktrees/
docs/.DS_Store
